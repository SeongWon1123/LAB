class SoundService {
  const SoundService();

  Future<void> playButton() async {
    // Placeholder until original licensed sounds are added.
  }

  Future<void> playCare() async {
    // Placeholder until original licensed sounds are added.
  }
}
